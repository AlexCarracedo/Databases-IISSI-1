
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing grades
### This query is used in several of the below tests it should be executed first.
# @name grades
GET {{BASE}}/grades

### Login a(n) employees and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing grades by its id.
### positive test 
@graId = {{grades.response.body.0.gradeId}}
GET {{BASE}}/grades/{{graId}} 
Content-Type: application/json 


### Test 02: Try get one existing grades by its nonexistent id.
### negative test 
GET {{BASE}}/grades/999999999
Content-Type: application/json 


### Test 03: Add a new grades successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newgrades
POST {{BASE}}/grades 
Content-Type: application/json 
Token: {{token}} 

{
	"value": ###REPLACE###,
	"gradeCall": ###REPLACE###,
	"withHonours": ###REPLACE###,
	"studentId": ###REPLACE###,
	"groupId": ###REPLACE###
} 

### Check the created grades

@newgradesid = {{newgrades.response.body.lastId}}
GET {{BASE}}/grades/{{newgradesid}}
Content-Type: application/json 

### Test 04: Add a new grades without a session token
### Negative test 
POST {{BASE}}/grades 
Content-Type: application/json 

{ 
	"value": ###REPLACE###,
	"gradeCall": ###REPLACE###,
	"withHonours": ###REPLACE###,
	"studentId": ###REPLACE###,
	"groupId": ###REPLACE###
}

### Test 05: Modify an existing grades 
### Positive test 
@graId = {{grades.response.body.0.gradeId}} 
PUT {{BASE}}/grades/{{graId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"value": ###REPLACE###,
	"gradeCall": ###REPLACE###,
	"withHonours": ###REPLACE###,
	"studentId": ###REPLACE###,
	"groupId": ###REPLACE###
}

### Check the modified grades

GET {{BASE}}/grades/{{newgradesid}}
Content-Type: application/json 

### Test 06: Try to modify an existing grades without a session token
### Negative test
@graId = {{grades.response.body.0.gradeId}}
PUT {{BASE}}/grades/{{graId}}
Content-Type: application/json

{
	"value": ###REPLACE###,
	"gradeCall": ###REPLACE###,
	"withHonours": ###REPLACE###,
	"studentId": ###REPLACE###,
	"groupId": ###REPLACE###
}

### Test 07: Delete an existing grades
### Positive test

### Create a new grades, which we will delete
# @name createdgraToDelete
POST {{BASE}}/grades
Content-Type: application/json
Token: {{token}}

{
	"value": ###REPLACE###,
	"gradeCall": ###REPLACE###,
	"withHonours": ###REPLACE###,
	"studentId": ###REPLACE###,
	"groupId": ###REPLACE###
}

### Check the created grades
@gradelId = {{createdgraToDelete.response.body.lastId}}
GET {{BASE}}/grades/{{gradelId}}
Content-Type: application/json 

### Delete the grades
DELETE {{BASE}}/grades/{{gradelId}}
Token: {{token}}

### Check the deleted grades
GET {{BASE}}/grades/{{gradelId}}
Content-Type: application/json 

### Test 08: Try to delete a grades without a session token
### Negative test
DELETE {{BASE}}/grades/{{graId}}

